<%- include('../partials/head.ejs'); -%>

  <div class="flex items-center justify-between p-4">
    <h1 class="text-2xl font-semibold">Seja bem vindo!</h1>
    <a class="bg-red-400 text-white px-4 py-1 rounded-sm" href="/auth/logout">Logout</a>
  </div>


  <div class="flex flex-col m-auto gap-2 p-8 sm:flex-row sm:items-center sm:gap-6 sm:py-4 bg-cyan-100 w-md rounded-md">
    <img class="mx-auto block h-24 rounded-full sm:mx-0 sm:shrink-0" src="<%= user.picture %>" alt="" />
    <div class="space-y-2 text-center sm:text-left">
      <div class="space-y-0.5">
        <p class="text-lg font-semibold text-black"><%= user.firstName %></p>
        <p class="font-medium text-gray-500"><%= user.email %></p>
      </div>
    </div>
  </div>

  <%- include('../partials/footer.ejs'); -%>